extends ../layout

block content
    div(class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8")

        //- Header
        div(class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-8")
            div
                h1(class="text-2xl sm:text-3xl font-bold text-gray-900")
                    | Lịch sử nộp bài
                p(class="text-gray-500 mt-1")
                    | Tất cả bài bạn đã nộp và kết quả chấm

            a(
                href="/practice"
                class="inline-flex items-center gap-2 px-4 py-2.5 bg-indigo-600 text-white rounded-lg text-sm font-medium hover:bg-indigo-700 transition"
            )
                i(data-lucide="pencil", class="w-4 h-4")
                | Luyện tập

        //- Empty state
        if !submissions || submissions.length === 0
            div(class="bg-white border border-gray-200 rounded-2xl py-16 text-center")
                div(class="w-14 h-14 rounded-full bg-gray-100 text-gray-400 flex items-center justify-center mx-auto")
                    i(data-lucide="inbox", class="w-6 h-6")
                h2(class="font-semibold text-gray-900 mt-4")
                    | Chưa có bài nộp nào
                p(class="text-sm text-gray-500 mt-1")
                    | Bắt đầu làm bài và nộp để xem kết quả ở đây.
                a(
                    href="/practice"
                    class="inline-flex items-center gap-2 mt-5 px-4 py-2.5 bg-indigo-600 text-white rounded-lg text-sm font-medium hover:bg-indigo-700"
                )
                    i(data-lucide="play", class="w-4 h-4")
                    | Làm bài ngay

        else
            //- Table
            div(class="bg-white border border-gray-200 rounded-2xl shadow-sm overflow-hidden")
                div(class="overflow-x-auto")
                    table(class="w-full text-sm")
                        thead(class="bg-gray-50")
                            tr
                                th(class="px-5 py-3 text-left font-semibold text-gray-600")
                                    | Bài học
                                th(class="px-5 py-3 text-left font-semibold text-gray-600")
                                    | Điểm
                                th(class="px-5 py-3 text-left font-semibold text-gray-600")
                                    | Số lỗi
                                th(class="px-5 py-3 text-left font-semibold text-gray-600")
                                    | Model
                                th(class="px-5 py-3 text-left font-semibold text-gray-600")
                                    | Thời gian
                                th(class="px-5 py-3 text-left font-semibold text-gray-600")
                                    | Trạng thái
                                th(class="px-5 py-3 text-right font-semibold text-gray-600")
                                    | Hành động

                        tbody(class="divide-y divide-gray-100")
                            each s in submissions
                                tr(class="hover:bg-gray-50")
                                    //- Bài học
                                    td(class="px-5 py-4")
                                        if s.lessonId && s.lessonId.title
                                            div
                                                p(class="font-medium text-gray-900")
                                                    = s.lessonId.title
                                                if s.subjectId && s.subjectId.name
                                                    p(class="text-xs text-gray-500 mt-0.5")
                                                        = s.subjectId.name
                                        else
                                            span(class="text-gray-400")
                                                | (Không có bài)

                                    //- Điểm
                                    td(class="px-5 py-4")
                                        if s.score !== null && s.score !== undefined
                                            span(class="font-bold text-lg text-gray-900")
                                                = s.score
                                            span(class="text-sm text-gray-500")
                                                |  / 10
                                        else
                                            span(class="text-gray-400")
                                                | —

                                    //- Số lỗi
                                    td(class="px-5 py-4")
                                        - var errCount = (s.grammar && Array.isArray(s.grammar.errors)) ? s.grammar.errors.length : 0;
                                        if errCount > 0
                                            span(class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-xs font-medium bg-red-100 text-red-700")
                                                i(data-lucide="circle-alert", class="w-3 h-3")
                                                = errCount + ' lỗi'
                                        else if s.status === 'graded'
                                            span(class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-xs font-medium bg-green-100 text-green-700")
                                                i(data-lucide="check-circle-2", class="w-3 h-3")
                                                | Không có lỗi
                                        else
                                            span(class="text-xs text-gray-400")
                                                | —

                                    //- Model
                                    td(class="px-5 py-4")
                                        if s.model
                                            span(class="text-xs font-mono px-2 py-1 rounded bg-indigo-50 text-indigo-700")
                                                = s.model
                                        else
                                            span(class="text-gray-400")
                                                | —

                                    //- Thời gian
                                    td(class="px-5 py-4 text-gray-500 text-xs")
                                        if s.submittedAt
                                            = new Date(s.submittedAt).toLocaleString('vi-VN')
                                        else if s.createdAt
                                            = new Date(s.createdAt).toLocaleString('vi-VN')
                                        else
                                            | —

                                    //- Trạng thái
                                    td(class="px-5 py-4")
                                        if s.status === 'graded'
                                            span(class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-xs font-medium bg-green-100 text-green-700")
                                                i(data-lucide="check-circle-2", class="w-3 h-3")
                                                | Đã chấm
                                        else if s.status === 'grading' || s.status === 'pending'
                                            span(class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-700")
                                                | Đang chấm
                                        else if s.status === 'failed'
                                            span(class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full text-xs font-medium bg-red-100 text-red-700")
                                                i(data-lucide="circle-alert", class="w-3 h-3")
                                                | Lỗi
                                        else
                                            span(class="inline-flex px-2.5 py-1 rounded-full text-xs font-medium bg-gray-100 text-gray-700")
                                                = s.status || 'Không xác định'

                                    //- Hành động
                                    td(class="px-5 py-4 text-right")
                                        a(
                                            href=`/submissions/${s._id}`
                                            class="inline-flex items-center gap-1 px-3 py-1.5 rounded-lg text-xs font-medium bg-indigo-50 text-indigo-700 hover:bg-indigo-100 transition"
                                        )
                                            i(data-lucide="eye", class="w-3.5 h-3.5")
                                            | Xem chi tiết