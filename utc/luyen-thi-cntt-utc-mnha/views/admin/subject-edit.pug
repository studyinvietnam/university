extends ../layout

block content
    div(class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8")

        //- Back
        div(class="mb-6")
            a(
                href="/admin/subjects"
                class="inline-flex items-center gap-1 text-sm text-gray-500 hover:text-gray-700"
            )
                i(data-lucide="arrow-left", class="w-4 h-4")
                | Quay lại danh sách

        //- Flash
        if success
            div(class="mb-6 p-4 rounded-xl bg-green-50 border border-green-200 text-green-800 text-sm")
                = success

        if error
            div(class="mb-6 p-4 rounded-xl bg-red-50 border border-red-200 text-red-800 text-sm")
                = error

        .bg-white.border.border-gray-200.rounded-2xl.p-6.shadow-sm
            h1(class="text-2xl font-bold text-gray-900 mb-6")
                | Chỉnh sửa môn học

            form(
                action=`/admin/subjects/${subject._id}/edit`
                method="POST"
                class="space-y-5"
            )
                div
                    label(class="block text-sm font-medium text-gray-700 mb-2")
                        | Tên môn học *
                    input(
                        type="text"
                        name="name"
                        required
                        value=subject.name
                        class="w-full px-3 py-2.5 border border-gray-300 rounded-lg outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
                    )

                div
                    label(class="block text-sm font-medium text-gray-700 mb-2")
                        | Mã môn học
                    input(
                        type="text"
                        name="code"
                        value=(subject.code || '')
                        class="w-full px-3 py-2.5 border border-gray-300 rounded-lg outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
                    )

                div
                    label(class="block text-sm font-medium text-gray-700 mb-2")
                        | Mô tả
                    textarea(
                        name="description"
                        rows="4"
                        class="w-full px-3 py-2.5 border border-gray-300 rounded-lg outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 resize-none"
                    )= subject.description || ''

                div(class="grid grid-cols-2 gap-4")
                    div
                        label(class="block text-sm font-medium text-gray-700 mb-2")
                            | Thứ tự
                        input(
                            type="number"
                            name="order"
                            value=(subject.order || 0)
                            class="w-full px-3 py-2.5 border border-gray-300 rounded-lg outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
                        )

                    div(class="flex items-end pb-2.5")
                        label(class="flex items-center gap-2 cursor-pointer")
                            input(
                                type="checkbox"
                                name="isPublished"
                                checked=subject.isPublished
                                class="w-4 h-4 rounded border-gray-300 text-indigo-600 focus:ring-indigo-500"
                            )
                            span(class="text-sm text-gray-700")
                                | Công khai cho sinh viên

                //- Audit info
                div(class="pt-4 border-t border-gray-100 text-xs text-gray-500 space-y-1")
                    if subject.createdBy
                        div
                            | Người tạo: #{subject.createdBy.name || subject.createdBy.email}
                    if subject.createdAt
                        div
                            | Tạo lúc: #{new Date(subject.createdAt).toLocaleString('vi-VN')}
                    if subject.updatedBy
                        div
                            | Người cập nhật cuối: #{subject.updatedBy.name || subject.updatedBy.email}
                    if subject.updatedAt
                        div
                            | Cập nhật lúc: #{new Date(subject.updatedAt).toLocaleString('vi-VN')}

                .flex.justify-end.gap-3.pt-4
                    a(
                        href="/admin/subjects"
                        class="px-4 py-2.5 rounded-lg border border-gray-300 text-gray-700 text-sm font-medium hover:bg-gray-50"
                    )
                        | Hủy

                    button(
                        type="submit"
                        class="px-4 py-2.5 rounded-lg bg-indigo-600 text-white text-sm font-medium hover:bg-indigo-700"
                    )
                        | Lưu thay đổi